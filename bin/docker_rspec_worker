#!/usr/bin/env ruby

require 'docker_test'
require 'docker_test/runner/rspec'

port = ENV['DOCKER_TEST_WORKER_PORT'] || 8000

runner = DockerTest::Runner::RSpec::DockerRunner.get_runner(ARGV)
worker = DockerTest::Worker.new(port, runner)
worker.start
