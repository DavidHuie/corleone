#!/usr/bin/env ruby

require 'docker_test'
require 'docker_test/runner/rspec'

port = ENV['DOCKER_TEST_WORKER_PORT'] || 8000

worker = DockerTest::Worker.new(port,
                                DockerTest::Runner::RSpec::DockerRunner)
worker.start
