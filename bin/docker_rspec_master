#!/usr/bin/env ruby

require 'docker_test'
require 'docker_test/runner/rspec'

port = ENV['DOCKER_TEST_MASTER_PORT'] || 7000

runner = DockerTest::Runner::RSpec.get_runner(ARGV)
master = DockerTest::Master.new(runner)
master.add_worker(8000)
master.start