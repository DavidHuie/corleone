#!/usr/bin/env ruby

Thread.abort_on_exception = true

require 'dt'
require 'dt/rspec'

server_uri = ENV['SERVER_URI'] || "druby://127.0.0.1:7000"

worker = DT::Worker.new(DT::Runner::RSpec, server_uri)
worker.block_until_server_ready
worker.start
